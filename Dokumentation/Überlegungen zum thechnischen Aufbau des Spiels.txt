Methoden:
	-render
	-update
		damit das Spiel nicht unterschiedlich unterschiedlich schnell läuft wird die update methode in einem bestimmten intervall aufgerufen und die render methode so oft wie möglich
		Tripple Buffering für SpeedImprovments 

Klassen:
	-Keyboard
		keeps track of keyboard events and saves them to a boolean array